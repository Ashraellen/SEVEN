# GREED / «Жадность» — RESUME TRIGGER AND ROLE TERMINOLOGY

Date: `2026-09-10`
Source chat: `MainMemorySystem-018`
Status: `AUTHOR-DIRECTED / RETAINED FOR NEXT SESSION / PROJECT NOT YET CREATED`

## 1. Resume trigger

The author explicitly established a natural-language resume trigger for the next working session:

`давай начнем делать грехи`

When the author uses this phrase (or an unmistakably equivalent instruction to resume the sins project), MainMemorySystem should NOT ask the author to reconstruct the discussion.

Instead it should:
1. restore the GREED / sins-series intake and handoff package;
2. verify fresh `Ashraellen/apm` `main` HEAD;
3. begin the project-initialization sequence authorized in that new turn;
4. create the project/app folder with the working project name `ГРЕХИ` unless the author changes the name at resume time;
5. create the GitHub project contour under an author-approved path;
6. create the main project chat / ORCHESTRATOR first;
7. then create EXECUTOR;
8. then create CONTROLLER;
9. route production through the project-main/orchestrator and author gates.

This file does NOT itself authorize project creation today. It only preserves the future trigger and intended naming/sequence.

## 2. Correct role terminology

The author explicitly corrected the role name:

`NOT WRITER`
`USE EXECUTOR`

For this future project, the canonical role vocabulary to use at initialization is:

`ORCHESTRATOR → EXECUTOR → CONTROLLER`

Definitions at this retained pre-project stage:
- `ORCHESTRATOR` = main project/directing chat; holds workflow state, routes tasks/results, manages versions/reviews/author gates.
- `EXECUTOR` = performs the assigned literary/research production task; does not independently redefine canon.
- `CONTROLLER` = independent editorial/factual/corpus-level reviewer; does not replace the Executor or become a second author.

Any earlier GREED intake wording that says `Writer`, `Writer / Executor`, or similar is historical/provisional language and is superseded for future project initialization by the explicit author correction `EXECUTOR`.

## 3. Project working name / folder intent

The author stated that when work resumes, the project folder should be called:

`ГРЕХИ`

Interpretation at this boundary:
- `ГРЕХИ` = author-directed working project/folder name for initialization;
- final public series title remains a separate naming/branding decision unless the author explicitly equates them later;
- Book I working title remains `Жадность / GREED`;
- the series remains a research-through-fiction project about seven human vulnerabilities, not a theological project.

## 4. Current stop boundary

Today the author explicitly paused this project discussion and may use MainMemorySystem for unrelated topics.

Therefore:
`PROJECT CREATION TODAY = NO`
`GITHUB PROJECT ROOT TODAY = NO`
`APP PROJECT/FOLDER TODAY = NO`
`ORCHESTRATOR TODAY = NO`
`EXECUTOR TODAY = NO`
`CONTROLLER TODAY = NO`

Resume only on a later explicit author instruction.

Preserve:
`CAPTURE != CANON`
`PROPOSED != ACCEPTED`
`RETENTION != CANON`
